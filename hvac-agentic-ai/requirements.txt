# Core stack for the MCP server, agents, and orchestration API.
# vLLM is intentionally NOT here — install it separately following README.md,
# since it needs to match your CUDA/driver version and is much larger.

mcp==1.9.4
fastapi==0.141.1
uvicorn[standard]==0.52.0
httpx==0.28.1
openai==2.50.0
pydantic==2.13.4
python-dotenv==1.2.2
